[package]
name = "blended_dataset_loop"
version = "0.1.0"
edition = "2021"

[lib]
name = "blended_dataset_loop"
crate-type = ["cdylib"]

[dependencies]
argminmax = "0.6"
serde = {version="1.0", features=["derive"]}
serde_json = "1.0"
tqdm = "0.6"
